import React from "react";
import Layout from "../../components/Layout/Layout";

interface ITicketsPage {}

const TicketsPage: React.FC<ITicketsPage> = React.memo(() => {
  return <Layout>TicketsPage</Layout>;
});

export default TicketsPage;
